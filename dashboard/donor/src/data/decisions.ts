// src/data/decisions.ts — РЕШЕНИЯ одинокого строителя. TASK-0155: затычка, пусто.
// Решение — зафиксированный выбор (архитектурный, регламентный, по контуру).
// После наполнения и компиляции выставьте window.MEMOK.decisions — дашборд подхватит.

/** Решение строителя. */
export interface Decision {
  /** Сквозной id: d-001, d-002, … */
  id: string;
  /** Дата принятия, YYYY-MM-DD. */
  date: string;
  /** Суть решения (коротко). */
  title: string;
  /** Статус решения. */
  status: "принято" | "в силе" | "отменено";
}

export const decisions: Decision[] = [];
