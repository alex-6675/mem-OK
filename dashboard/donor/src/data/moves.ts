// src/data/moves.ts — ДВИЖЕНИЯ одинокого строителя. TASK-0155: затычка, пусто.
// Движение — факт изменения в контуре (карточка, публикация, слияние, приём волонтёра).
// После наполнения и компиляции выставьте window.MEMOK.moves — дашборд подхватит.

/** Движение — запись в журнале строительства/работы. */
export interface Move {
  /** Сквозной id: m-0001, m-0002, … */
  id: string;
  /** Unix, сек. */
  ts: number;
  /** Что произошло (коротко, для человека). */
  text: string;
  /** Координата: карточка / файл / отчёт (необязательно). */
  target?: string;
}

export const moves: Move[] = [];
