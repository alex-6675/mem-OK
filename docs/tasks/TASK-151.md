тест маркировки ОК План А — RunEdgeCdp.ps1 (профили .edge_test_*, порты 9222–9226), по регламенту действий: чужая территория — чтение, результаты — себе; одно движение — один отчёт. Отчёт ляжет четвёртой строкой в «движения» дашборда.